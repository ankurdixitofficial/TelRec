services:
  - type: web
    name: resume-builder-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn api.job_api:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.12 